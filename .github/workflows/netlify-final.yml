name: Final Anonymous Netlify Deploy

on:
  push:
    branches:
      - agent/dynamic-tire-load-transfer

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'

      - name: Install dependencies
        run: npm install

      - name: Build production bundle
        run: npm run build

      - name: Deploy anonymously to Netlify
        run: |
          npx --yes netlify-cli@27.0.1 deploy --allow-anonymous --dir=dist --no-build --json | tee netlify-final.json
          cat netlify-final.json

      - name: Upload deploy metadata
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: netlify-final
          path: netlify-final.json
          if-no-files-found: ignore
          retention-days: 1
