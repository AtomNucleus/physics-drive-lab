name: Temporary 2025 M5 Calibration

on:
  push:
    branches:
      - agent/dynamic-tire-load-transfer

permissions:
  contents: write

jobs:
  calibrate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: agent/dynamic-tire-load-transfer
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.14.0'

      - name: Install dependencies
        run: npm install

      - name: Apply M5 production calibration
        run: |
          python3 scripts/m5_patch.py
          if ! git diff --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add src/physics/m5G90.ts src/physics/Vehicle.ts src/physics/Differential.ts src/physics/vehiclePresets.ts src/App.tsx
            git commit -m "Calibrate default drive to 2025 BMW M5 G90"
            git push origin HEAD:agent/dynamic-tire-load-transfer
          fi

      - name: Production build
        run: npm run build

      - name: Run M5 dynamics benchmark
        run: npx tsx scripts/m5_bench.ts | tee m5-calibration.log

      - name: Upload calibration results
        uses: actions/upload-artifact@v4
        with:
          name: m5-calibration
          path: |
            m5-calibration.json
            m5-calibration.log
          retention-days: 1
